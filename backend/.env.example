PORT=5000

# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=train_booking

# Redis
REDIS_URL=redis://localhost:6379

# Starting wallet balance given to every new profile (dummy currency)
INITIAL_WALLET_BALANCE=1000
